{
  "name": "detectiveBackend",
  "version": "1.0.0",
  "scripts": {
    "start": "pm2 start --watch",
    "log": "pm2 log detectiveBackend",
    "stop": "pm2 stop detectiveBackend && pm2 delete detectiveBackend",
    "build": "pm2 start --env production"
  },
  "dependencies": {
    "csprng": "^0.1.2",
    "jshashes": "^1.0.7",
    "jsonwebtoken": "^8.4.0",
    "koa": "^2.6.2",
    "koa-body": "^4.0.6",
    "koa-router": "^7.4.0",
    "koa-static": "^5.0.0",
    "mongodb": "^3.1.10",
    "mongoose": "^5.3.14",
    "path": "^0.12.7"
  }
}
